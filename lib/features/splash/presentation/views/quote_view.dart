import 'package:flutter/material.dart';
import 'package:move_on/features/splash/presentation/views/widgets/quote_view_body.dart';

class QuoteView extends StatelessWidget {
  const QuoteView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: QuoteViewBody());
  }
}
